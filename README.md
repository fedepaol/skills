# Claude Code Skills

Custom skills for [Claude Code](https://claude.ai/claude-code).

## Setup

Run the deploy script to install skills into `~/.claude/skills/`:

```bash
./deploy.sh
```

## Adding a New Skill

Create a directory with a `SKILL.md` file and run `./deploy.sh` again.
